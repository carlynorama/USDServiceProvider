import XCTest
@testable import USDServiceProvider

final class USDServiceProviderTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(USDServiceProvider().text, "Hello, World!")
    }
}
